import React from 'react';
import { renderWithRedux } from 'tests/renderWithRedux';
import { fireEvent, screen } from '@testing-library/react';
import {{pascalCase componentName}} from './{{dashCase componentName}}';

describe('{{pascalCase componentName}}', () => {

  const render{{pascalCase componentName}} = () => {
    return renderWithRedux(<{{pascalCase componentName}} />, undefined);
  };

  it('render {{pascalCase componentName}}', async () => {
    render{{pascalCase componentName}}();
  });

});